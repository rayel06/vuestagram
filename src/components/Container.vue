<template>
    <div>
        <div v-if="step == 0">
            <Post v-for="(a,i) in data" :key="i" :data="a"/>
        </div>
    <!-- 필터선택페이지 -->
    <div v-if="step == 1" class="upload-image" :class="filterSelected" :style="`background-image: url(${img})`"></div>
    <div v-if="step == 1" class="filters">
        <FilterBox v-for="filter in filters" :key="filter" :img="img" :filter="filter">
            {{filter}}
        </FilterBox>
    </div>

    <!-- 글작성페이지 -->
    <div class="upload-image" :class="filterSelected"  v-if="step == 2" :style="`background-image: url(${img})`"></div>
        <div v-if="step == 2" class="write">
            <textarea @input="$emit('writeValue', $event.target.value)" class="write-box" placeholder="write!"></textarea>
        </div>
    </div>
</template>

<script>
import Post from './Post'
import FilterBox from './FilterBox'
export default {
    name: 'Contaner',
    data(){
        return{
            filters : [ "aden", "_1977", "brannan", "brooklyn", "clarendon", "earlybird", "gingham", "hudson", 
                        "inkwell", "kelvin", "lark", "lofi", "maven", "mayfair", "moon", "nashville", "perpetua", 
                        "reyes", "rise", "slumber", "stinson", "toaster", "valencia", "walden", "willow", "xpro2"]
        }   
    },
    components: {
        Post,
        FilterBox,
    },
    props: {
        data: Array,
        step: Number,
        img: String,
        filterSelected: String,

    },
}
</script>

<style>
.upload-image{
width: 100%;
height: 450px;
background: cornflowerblue;
background-size : cover;
}
.filters{
overflow-x:scroll;
white-space: nowrap;
}
.filter-1 {
width: 100px;
height: 100px;
background-color: cornflowerblue;
margin: 10px 10px 10px auto;
padding: 8px;
display: inline-block;
color : white;
background-size: cover;
}
.filters::-webkit-scrollbar {
height: 5px;
}
.filters::-webkit-scrollbar-track {
background: #f1f1f1; 
}
.filters::-webkit-scrollbar-thumb {
background: #888; 
border-radius: 5px;
}
.filters::-webkit-scrollbar-thumb:hover {
background: #555; 
}
.write-box {
border: none;
width: 90%;
height: 100px;
padding: 15px;
margin: auto;
display: block;
outline: none;
}
</style>